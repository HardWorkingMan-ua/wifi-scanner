name: Nightly Build

on:
  schedule:
    # Запуск щодня о 00:00 UTC
    - cron: '0 0 * * *'
  workflow_dispatch: # Дозволяє запустити збірку вручну через вкладку Actions

jobs:
  build:
    runs-on: ubuntu-latest
    
    # Потрібні дозволи для створення релізу
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libnl-3-dev libnl-genl-3-dev

      - name: Build Project
        run: make

      - name: Create/Update Nightly Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: nightly
          name: "Nightly Build 🌙"
          body: |
            Автоматична нічна збірка WiFi Scanner.
            
            **Дата збірки:** ${{ github.event.head_commit.timestamp }}
            **Коміт:** ${{ github.sha }}
            
            ### Як використовувати
            1. Завантажте бінарний файл `wifi-scanner`
            2. Зробіть його виконуваним: `chmod +x wifi-scanner`
            3. Запустіть з правами root: `sudo ./wifi-scanner -i wlan0`
          files: wifi-scanner
          prerelease: true
          make_latest: true
          fail_on_unmatched_files: true
